import{mkdirSync,readFileSync,renameSync,writeFileSync}from'node:fs';import{dirname}from'node:path';export class JsonHoldStore{constructor(file){this.file=file}load(){try{const s=JSON.parse(readFileSync(this.file,'utf8'));return{holds:s.holds??[],audit:s.audit??[]}}catch(e){if(e.code==='ENOENT')return{holds:[],audit:[]};throw e}}save(s){mkdirSync(dirname(this.file),{recursive:true});const t=`${this.file}.tmp`;writeFileSync(t,JSON.stringify(s,null,2));renameSync(t,this.file)}}
